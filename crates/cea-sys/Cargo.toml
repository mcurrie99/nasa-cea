[package]
name = "cea-sys"
version = "0.1.0"
edition = "2024"
links = "cea"

[features]
# Build CEA from a vendored checkout (recommended for dev/CI)
vendored = []

[build-dependencies]
bindgen = "0.72.1"
cmake = "0.1"

[dependencies]
